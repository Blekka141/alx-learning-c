# 0x03. C - Debugging

## Overview
This project focused on the process of debugging in C programming. It took place from October 10, 2023, to October 13, 2023. Debugging is essential for finding and fixing errors that prevent software from running correctly.

## Resources

### Read or Watch:
- Debugging
- Rubber Duck Debugging

## Learning Objectives
By the end of this project, you should be able to explain:
- What debugging is
- Various methods of debugging manually
- How to interpret error messages effectively

## Requirements

### General
- **Allowed editors:** vi, vim, emacs
- **Compilation:** Ubuntu 20.04 LTS using gcc with `-Wall -Werror -Wextra -pedantic -std=gnu89`
- All files must end with a new line
- Code should adhere to the Betty style, checked using `betty-style.pl` and `betty-doc.pl`
- A README.md file at the root of the folder of this project is mandatory

## Quiz Questions

### Question #0
The following code gives this output. What is the error?

```c
#include <stdio.h>

/**
 * main - debugging example
 * Return: 0
 */
int main(void)
{
    int i;
    int j;
    int k;

    i = 0;
    j = 1000;
    while (i < j)
    {
        k = j / 98;
        i = i + k;
        printf("%d\n", i);
        j == j - 1;
    }

    return (0);
}
